# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# User-supplied playlists — never bundle/redistribute channels
*.m3u
*.m3u8

# Compound Engineering machine-local config
.compound-engineering/*.local.yaml

# Local macOS signing artifact (account/device-specific, regenerable)
src-tauri/*.provisionprofile

# Release credentials (Developer ID + notarization) — never commit
.env.release
